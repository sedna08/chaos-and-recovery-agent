# .env.example

# The base URL for the backend inventory API.
# - Local development: http://localhost:8080 (or whatever port your backend runs on)
# - Kubernetes/Minikube: http://inventory-api (Internal K8s DNS resolves the service name)
INVENTORY_API_URL=http://inventory-api